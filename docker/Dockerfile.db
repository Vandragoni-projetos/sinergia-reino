# SinergIA Core - Imagem do banco (contexto = raiz do projeto)
FROM mariadb:10.11

# Carga inicial para 1ª instalação (schema + seed mínimo)
COPY Base_de_Dados_Instalacao.sql /docker-entrypoint-initdb.d/init.sql

RUN chmod 644 /docker-entrypoint-initdb.d/init.sql
